version: '3.5'

services:
  consignementMS:
    restart: always
    container_name: "shippinggo_vesselms"
    build: .
    ports:
      - 50051 #exposing this port on the docker network only not host
    links:
      - datastore
    depends_on: 
      - datastore
    networks:
      - shippinggo-backend-tier #network to communicate with the other services
      - vesselms-tier #internal network to communicate with the DB
    environment: 
      DB_HOST: "mongodb://datastore:27017"
      MICRO_ADDRESS: ":50051"
  
  datastore:
    image: mongo:latest
    container_name: "datastore"
    environment: 
      - MONGO_DATA_DIR=/data/db
      - MONGO_LOG_DIR=/dev/null
    volumes:
      - ./data/db:/data/db #ensure data persistence between restarts
    networks: 
      - consignement-tier
    ports:
      - 27017
    command: mongod --logpath=/dev/null
networks: 
  consignement-tier:
    name: vesselms-tier
  shippinggo-backend-tier:
    name: shippinggo-backend-tier
